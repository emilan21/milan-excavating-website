{
  "name": "milan-excavating-spacetimedb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spacetime build",
    "generate": "spacetime generate --lang typescript --out-dir ../src/module_bindings --module-path . --include-private --yes"
  },
  "dependencies": {
    "spacetimedb": "2.10.*"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
